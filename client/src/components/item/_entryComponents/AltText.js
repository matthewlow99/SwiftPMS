import React from "react"

function AltText({text, color}){
    return ( <div className={'flexRowAlignCenter'} style={{ flex: 3 }}>
        <p style={{ color: 'black' }}>{null}</p>
    </div>)
}

export default AltText